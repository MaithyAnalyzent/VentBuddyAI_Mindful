# VentBuddy AI Frontend

React/Vite frontend for the standalone VentBuddy AI wellness companion.

## Environment

Copy `.env.example` to `.env` and set:

```bash
VITE_API_BASE_URL=http://localhost:8000/api
```

## Commands

```bash
npm install
npm run dev
npm run build
```
